# 内置终端组件（SSH小电脑）

## 定位
每个客户实例内置，开箱即用，无需配置。
等同于 Claude Code 的终端能力，用中文指令驱动。

## 能力
- 执行 shell 命令
- 读写文件
- 运行代码
- Docker 容器操作
- 网络请求

## 触发方式
用户说"帮我在服务器上..."、"运行..."、"查看..."自动调用。

## 样式
```
位置：主界面右侧抽屉，宽 480px，可全屏
背景：#0A0A0F
字体：JetBrains Mono 13px
提示符：[VAST]$ 
颜色：绿色输出 #00C97A，错误红色 #FF4D6A
```
